# ─────────────────────────────────────────────────────────────────────────────
# User access control — keyed by Google account email address.
#
# When someone logs in with Google, their email is checked against this list.
# If not found, access is denied.
#
# locations = ["all"]          → sees every property (owner)
# locations = ["ID1", "ID2"]  → sees only those location IDs (manager)
# ─────────────────────────────────────────────────────────────────────────────

[users."regulskiu@gmail.com"]
locations = ["all"]

[users."lukasz@imprv.ai"]
locations = ["8962787873732607458"]
